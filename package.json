{
  "name": "doctor-app",
  "version": "1.0.0",
  "scripts": {
    "start": "uvicorn Backend.main:app --host 0.0.0.0 --port $PORT",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  },
  "engines": {
    "node": "18.x"
  }
}
