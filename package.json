{
  "name": "doctor-app",
  "version": "1.0.0",
  "scripts": {
    "start": "sh start.sh",
    "build": "cd frontend && npm install --omit=dev && npm run build",
    "install-deps": "pip install -r Backend/requirements.txt && cd frontend && npm install --omit=dev"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=9.0.0"
  }
}
